import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from '@stencil/core';
export const ModusTableCellLinkElement = ({ link, onLinkClick }) => {
  return (h("div", { class: "cell-link wrap-text", onClick: () => onLinkClick(link) }, link.display));
};
