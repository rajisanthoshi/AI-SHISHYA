import { h, // eslint-disable-line @typescript-eslint/no-unused-vars
 } from '@stencil/core';
import { IconChevronDownThick } from '../../../icons/icon-chevron-down-thick';
import { IconChevronUpThick } from '../../../icons/icon-chevron-up-thick';
const ModusTableCellExpandIcons = ({ cellIndex, row }) => {
  return (h("span", { class: "expand", style: { paddingLeft: `${cellIndex === 0 ? row.depth * 2 : 0}rem` }, onClick: row.getToggleExpandedHandler() }, cellIndex === 0 && row.getCanExpand() ? (row.getIsExpanded() ? (h(IconChevronUpThick, { size: '24' })) : (h(IconChevronDownThick, { size: '24' }))) : ('')));
};
export default ModusTableCellExpandIcons;
