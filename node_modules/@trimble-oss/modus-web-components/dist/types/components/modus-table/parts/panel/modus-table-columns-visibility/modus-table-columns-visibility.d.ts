import { Table } from '@tanstack/table-core';
import { JSX } from '../../../../../stencil-public-runtime';
import { ModusTableColumnsVisibilityOptions } from '../../../models/modus-table.models';
export declare class ModusTableColumnsVisibility {
  /** Table data. */
  table: Table<unknown>;
  /** Column visibility options */
  columnsVisibility: ModusTableColumnsVisibilityOptions;
  showDropdown: boolean;
  menuIconContainerRef: HTMLDivElement;
  toggleDropdown: (show: boolean) => void;
  columnsVisibilityState: Map<string, boolean>;
  private refItemContent;
  applyColumnsVisibility(): void;
  closeDropdown(): void;
  handleApplyKeyDown(event: KeyboardEvent): void;
  /**
   * Handling columns dropdown keyboard(arrowUp/arrowDown) navigation
   * @param event keyboard event
   * @param columnIndex column index
   */
  handleColumnItemKeyDown(event: KeyboardEvent, columnIndex: number): void;
  /**
   * Adding column item refs(not disabled)
   */
  handleRefColumnItemContent(el: HTMLDivElement, i: number, requiredColumn: boolean): void;
  private toggleColumnVisibility;
  /**
   * Returns true if the column is required column
   */
  private checkIfRequiredColumn;
  renderColumnsChecklist(): JSX.Element | null;
  renderCancelApplyButtons(): JSX.Element | null;
  render(): void;
}
