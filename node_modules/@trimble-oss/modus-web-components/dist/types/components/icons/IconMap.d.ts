import { FunctionalComponent } from '../../stencil-public-runtime';
export interface IconProps {
  color?: string;
  onClick?: (event?: any) => void;
  size?: string;
  pressed?: boolean;
}
interface IconMapProps extends IconProps {
  icon: string;
  imageOptions?: {
    [key: string]: string;
  };
}
export declare const IconMap: FunctionalComponent<IconMapProps>;
export {};
