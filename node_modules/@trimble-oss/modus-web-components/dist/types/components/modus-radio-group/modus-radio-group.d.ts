import { EventEmitter } from '../../stencil-public-runtime';
import { RadioButton } from './modus-radio-button';
export declare class ModusRadioGroup {
  /** The radio group's aria-label. */
  ariaLabel: string | null;
  /** The ID of the checked radio button. */
  checkedId: string;
  /** The radio button group name. */
  name: string;
  /** The radio buttons to render. */
  radioButtons: RadioButton[];
  /** Fires on radio button click. */
  buttonClick: EventEmitter<string>;
  componentWillLoad(): void;
  private handleButtonClick;
  private handleButtonKeydown;
  private setCheckedIdAndUpdateRadioButtons;
  render(): unknown;
}
